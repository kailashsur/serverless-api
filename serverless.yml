service: express-api
frameworkVersion: '3'

plugins:
  - serverless-offline
  - serverless-dotenv-plugin
provider:
  name: aws
  runtime: nodejs20.x
  region: ap-south-1
  memorySize: 128
  environment:
functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'